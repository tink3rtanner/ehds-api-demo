"""External FHIR data source adapters.

Each module here fetches resources from a real EHR (Epic, Cerner, ...) and
transforms them into IPS-shaped resources that the local store + bundler
can already compile into EU EPS documents.
"""
